[package]
name = "volt-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "VOLT — Virtual Ohmic Logic Testbench core library"

[dependencies]
rand = "0.8"
rand_distr = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
